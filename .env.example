# Pañol v2.0 — Variables de entorno
# Copiar este archivo como .env y completar los valores

# Base de datos
DB_PASSWORD=panol123_CAMBIAR

# Seguridad (generar con: python -c "import secrets; print(secrets.token_hex(32))")
SECRET_KEY=tu_clave_secreta_muy_larga_aqui
JWT_SECRET_KEY=tu_jwt_secret_muy_largo_aqui

# Modo de depuración (false en producción)
DEBUG=false

# Empresa
COMPANY_NAME=Mi Empresa S.A.

# Email (opcional)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=tu@email.com
SMTP_PASSWORD=tu_contraseña_app

# WhatsApp Business (opcional)
WHATSAPP_API_TOKEN=
WHATSAPP_PHONE_ID=
